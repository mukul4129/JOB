<h1>Registrations for <%= link_to @naukri.name, @naukri %></h1>
<ul class="registrations">
  <% @registrations.each do |r| %>
    <li>
      <%= link_to r.user.name, r.user %>
    </li>
  <% end %>
</ul>