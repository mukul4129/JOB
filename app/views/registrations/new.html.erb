<h1>Registration for <%= link_to @naukri.name, @naukri %></h1>

<%= form_with(model: [@naukri, @registration], local: true) do |f| %>
  <%= render "shared/errors", object: @registration %>

  <%= f.submit %>
<% end %>